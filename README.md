# Dan's Teleprompter

A Python teleprompter application.

## Development

Install the project in editable mode and run the test suite:

```powershell
python -m pip install -e ".[dev]"
python -m pytest
```
